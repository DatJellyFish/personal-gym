services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: forgefit
    restart: unless-stopped
    ports:
      - "8088:3000"
    volumes:
      - ./data:/app/data
